---
title: 游戏下载
---

import GameCard from '@site/src/components/GameCard/GameCard';
import { GAMES } from '@site/src/pages/games';

<style>{`
  .games-container {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(240px, 1fr));
    gap: 1.5rem;
    padding: 2rem 1rem;
    max-width: 1200px;
    margin: 0 auto;
  }
  
  @media (max-width: 480px) {
    .games-container {
      grid-template-columns: 1fr;
      padding: 1rem;
    }
  }
`}</style>

# 游戏中心

<div className="games-container">
  {GAMES.map((game) => (
    <GameCard key={game.title} {...game} />
  ))}
</div>