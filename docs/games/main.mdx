---
slug: /main
title: 游戏下载
---

import GameCard from '../src/components/GameCard/GameCard';
import { GAMES } from '../src/pages/games';

// 确保所有JSX标签正确闭合
const GamesPage = () => (
  <main>
    {/* 样式区块必须完全闭合 */}
    <style>{`
      .games-container {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(240px, 1fr));
        gap: 1.5rem;
        padding: 2rem 1rem;
        max-width: 1200px;
        margin: 0 auto;
      }
      
      @media (max-width: 480px) {
        .games-container {
          grid-template-columns: 1fr;
          padding: 1rem;
        }
      }
    `}</style>

    {/* 标题使用JSX语法 */}
    <h1>游戏中心</h1>

    {/* 确保数组遍历完全闭合 */}
    <div className="games-container">
      {GAMES.map((game) => (
        <GameCard 
          key={game.title}
          title={game.title}
          icon={game.icon}
          url={game.url}
        />  {/* 注意组件闭合 */}
      ))}
    </div>
  </main>
);

export default GamesPage; // 必须的默认导出